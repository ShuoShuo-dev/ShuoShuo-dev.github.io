---
title: 24.CSS列表样式
category: CSS
tags: [CSS, 列表, list-style]
description: CSS列表样式详解
date: 2026-04-15
---

# 24.CSS列表样式

## list-style 属性

```css
ul, ol {
  list-style: disc inside;
  /* list-style-type position image */
}
```

## list-style-type 类型

```css
/* 无序列表 */
list-style-type: disc;     /* 实心圆 */
list-style-type: circle;   /* 空心圆 */
list-style-type: square;   /* 实心方块 */
list-style-type: none;     /* 无标记 */

/* 有序列表 */
list-style-type: decimal;           /* 数字 */
list-style-type: decimal-leading-zero; /* 01 02 03 */
list-style-type: lower-alpha;       /* a b c */
list-style-type: upper-alpha;       /* A B C */
list-style-type: lower-roman;       /* i ii iii */
list-style-type: upper-roman;        /* I II III */
```

## list-style-position 位置

```css
list-style-position: outside;  /* 标记在列表项外 */
list-style-position: inside;   /* 标记在列表项内 */
```

## list-style-image 图片

```css
list-style-image: url('bullet.png');
list-style-image: none;
```

## 常用列表样式

### 1. 无样式列表
```css
.list-none {
  list-style: none;
  padding-left: 0;
  margin: 0;
}
```

### 2. 自定义列表标记
```css
.custom-list {
  list-style: none;
  padding-left: 20px;
}

.custom-list li::before {
  content: '✓';
  color: #2ecc71;
  margin-right: 10px;
}
```

### 3. 数字列表
```css
.numbered-list {
  list-style: decimal-leading-zero;
  padding-left: 25px;
}
```

### 4. 链接列表
```css
.link-list li {
  list-style: none;
  border-bottom: 1px solid #eee;
}

.link-list a {
  display: block;
  padding: 10px 0;
  color: #3498db;
  text-decoration: none;
}
```

### 5. 水平列表
```css
.horizontal-list {
  list-style: none;
  display: flex;
  gap: 20px;
}
```

### 6. 图标列表
```css
.icon-list li {
  list-style: none;
  padding-left: 25px;
  position: relative;
}

.icon-list li::before {
  content: '▶';
  position: absolute;
  left: 0;
  color: #3498db;
}
```

### 7. 导航菜单
```css
.nav-menu {
  list-style: none;
  display: flex;
  gap: 2px;
}

.nav-menu li {
  background: #f5f5f5;
}

.nav-menu a {
  display: block;
  padding: 10px 20px;
  text-decoration: none;
  color: #333;
}
```
