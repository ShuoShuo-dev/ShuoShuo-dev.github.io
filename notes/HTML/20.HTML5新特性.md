---
title: 20.HTML5新特性
category: HTML
tags: [标签, HTML5, 新特性]
description: 全面了解HTML5新增的特性
date: 2024-01-01
---

# HTML5新特性

## HTML5简介

HTML5是HTML的第五个主要版本，相比HTML4有了重大改进，增加了许多新特性和API。

## 新增语义化标签

### 结构标签

```html
<header>头部</header>
<nav>导航</nav>
<main>主要内容</main>
<article>文章</article>
<section>章节</section>
<aside>侧边栏</aside>
<footer>底部</footer>
<figure>插图</figure>
<figcaption>插图说明</figcaption>
```

### 媒体标签

```html
<video>视频</video>
<audio>音频</audio>
<track>字幕轨道</track>
<source>媒体源</source>
<embed>嵌入内容</embed>
<object>对象</object>
```

### 其他新标签

```html
<meter>度量</meter>
<progress>进度条</progress>
<time>时间</time>
<mark>高亮</mark>
<details>详情折叠</details>
<summary>折叠标题</summary>
<dialog>对话框</dialog>
<canvas>画布</canvas>
<svg>矢量图形</svg>
```

## 表单增强

### 新增input类型

```html
<input type="email">    <!-- 邮箱 -->
<input type="url">      <!-- URL -->
<input type="tel">      <!-- 电话 -->
<input type="number">   <!-- 数字 -->
<input type="range">   <!-- 滑动条 -->
<input type="search">   <!-- 搜索框 -->
<input type="color">    <!-- 颜色选择 -->
<input type="date">    <!-- 日期 -->
<input type="time">    <!-- 时间 -->
<input type="datetime-local">  <!-- 本地日期时间 -->
<input type="month">    <!-- 月份 -->
<input type="week">     <!-- 周 -->
```

### 新增表单属性

```html
<input type="text" placeholder="提示文字">
<input type="text" autofocus>                    <!-- 自动聚焦 -->
<input type="text" required>                    <!-- 必填 -->
<input type="text" pattern="[A-Za-z]{3}">       <!-- 正则验证 -->
<input type="text" autocomplete="off">          <!-- 自动完成 -->
<input type="number" min="1" max="100">         <!-- 最小最大值 -->
<input type="number" step="2">                  <!-- 步长 -->
<input type="text" maxlength="10">              <!-- 最大长度 -->
<form novalidate>                              <!-- 禁止验证 -->
```

### 新增表单元素

```html
<!-- datalist输入建议列表 -->
<input type="text" list="suggestions">
<datalist id="suggestions">
    <option value="苹果">
    <option value="香蕉">
    <option value="橙子">
</datalist>

<!-- keygen密钥生成（已废弃） -->
<keygen name="key">

<!-- output输出 -->
<form oninput="result.value=parseInt(a.value)+parseInt(b.value)">
    <input type="number" id="a" value="0"> +
    <input type="number" id="b" value="0"> =
    <output name="result" for="a b"></output>
</form>
```

## 本地存储

### localStorage

```javascript
// 存储数据
localStorage.setItem('name', '张三');
localStorage.setItem('age', '25');

// 读取数据
let name = localStorage.getItem('name');

// 删除数据
localStorage.removeItem('name');

// 清空所有
localStorage.clear();
```

### sessionStorage

```javascript
// 类似localStorage，但会话结束即清除
sessionStorage.setItem('token', 'abc123');
```

### localStorage vs sessionStorage

| 特性 | localStorage | sessionStorage |
|------|--------------|----------------|
| 生命周期 | 永久 | 会话结束 |
| 大小 | 约5MB | 约5MB |
| 作用域 | 同源 | 同源+同窗口 |

## Canvas画布

```html
<canvas id="myCanvas" width="500" height="300"></canvas>

<script>
const canvas = document.getElementById('myCanvas');
const ctx = canvas.getContext('2d');

// 绘制矩形
ctx.fillStyle = 'red';
ctx.fillRect(50, 50, 200, 100);

// 绘制圆形
ctx.beginPath();
ctx.arc(200, 150, 50, 0, Math.PI * 2);
ctx.fillStyle = 'blue';
ctx.fill();

// 绘制文字
ctx.font = '20px Arial';
ctx.fillText('Hello Canvas', 200, 50);
</script>
```

## SVG图形

```html
<svg width="200" height="200">
    <!-- 矩形 -->
    <rect x="10" y="10" width="100" height="80" fill="red" />

    <!-- 圆形 -->
    <circle cx="150" cy="50" r="40" fill="blue" />

    <!-- 直线 -->
    <line x1="10" y1="120" x2="190" y2="120" stroke="green" stroke-width="2" />

    <!-- 文本 -->
    <text x="10" y="180" fill="purple">SVG文本</text>
</svg>
```

## 拖拽API

```javascript
// 拖拽元素
element.draggable = true;
element.ondragstart = (e) => {
    e.dataTransfer.setData('text/plain', e.target.id);
};

// 目标元素
target.ondragover = (e) => {
    e.preventDefault(); // 阻止默认行为
};

target.ondrop = (e) => {
    e.preventDefault();
    const id = e.dataTransfer.getData('text/plain');
    target.appendChild(document.getElementById(id));
};
```

## 地理定位

```javascript
navigator.geolocation.getCurrentPosition(
    (position) => {
        console.log('纬度:', position.coords.latitude);
        console.log('经度:', position.coords.longitude);
    },
    (error) => {
        console.error('获取位置失败:', error);
    }
);
```

## Web Workers

```javascript
// worker.js
self.onmessage = (e) => {
    const result = e.data * 2;
    self.postMessage(result);
};

// 主线程
const worker = new Worker('worker.js');
worker.postMessage(10);
worker.onmessage = (e) => {
    console.log('结果:', e.data);
};
```

## WebSocket

```javascript
const ws = new WebSocket('ws://example.com/socket');

// 连接成功
ws.onopen = () => {
    ws.send('Hello Server');
};

// 接收消息
ws.onmessage = (event) => {
    console.log('收到消息:', event.data);
};

// 连接关闭
ws.onclose = () => {
    console.log('连接已关闭');
};

// 发生错误
ws.onerror = (error) => {
    console.error('错误:', error);
};
```

## 其他新API

### 文件API

```javascript
const input = document.querySelector('input[type="file"]');
input.onchange = (e) => {
    const file = e.target.files[0];
    console.log('文件名:', file.name);
    console.log('文件大小:', file.size);
    console.log('文件类型:', file.type);
};
```

### 通知API

```javascript
if (Notification.permission === 'granted') {
    new Notification('标题', {
        body: '这是通知内容',
        icon: 'icon.png'
    });
} else {
    Notification.requestPermission();
}
```

### 全屏API

```javascript
// 全屏
document.documentElement.requestFullscreen();

// 退出全屏
document.exitFullscreen();

// 切换全屏
const toggle = () => {
    if (document.fullscreenElement) {
        document.exitFullscreen();
    } else {
        document.documentElement.requestFullscreen();
    }
};
```

### 页面可见性API

```javascript
document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
        console.log('页面已隐藏，暂停动画');
    } else {
        console.log('页面已显示，继续动画');
    }
});
```

## 浏览器兼容

使用特性检测：

```javascript
// 检测Canvas支持
const supportCanvas = () => !!document.createElement('canvas').getContext;

// 检测localStorage
const supportStorage = () => {
    try {
        localStorage.setItem('test', 'test');
        localStorage.removeItem('test');
        return true;
    } catch (e) {
        return false;
    }
};

// 检测WebSocket
const supportWebSocket = () => 'WebSocket' in window;
```

## HTML5声明

```html
<!DOCTYPE html>
<html lang="zh-CN">
```

## 总结

HTML5新特性包括：
1. **语义化标签**：更好的结构化
2. **表单增强**：更多输入类型和属性
3. **本地存储**：localStorage和sessionStorage
4. **Canvas和SVG**：强大的图形绘制
5. **多媒体**：原生video和audio
6. **拖拽**：原生拖拽API
7. **地理定位**：获取用户位置
8. **Web Workers**：后台线程
9. **WebSocket**：实时通信
10. **其他API**：文件、通知、全屏等
